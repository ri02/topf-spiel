<div class="container">

  <div class="head">
    <h1>NEUER BEGRIFF</h1>
    <p>Hier <strong><%= @game.number %></strong> Begriffe, Person, Bla Bla eingeben  </p>
  </div>

  <%= simple_form_for [@game, @word] do |f| %>
  <% @i.each do |i|%>
    <%= f.input :text, label: 'Hirnpfurz', as: :text, input_html: { multiple: true } %>
    <% end %>
    <%= f.submit 'Rein in den Topf', class: 'button' %>
  <% end %>
</div>
